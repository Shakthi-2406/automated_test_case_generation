graphviz


#include <iostream>
#include<bits/stdc++.h>

using namespace std;

int find_dp(vector<int>&a, vector<int>&b, vector<vector<int>>&dp, int rem, int ind, int n){
    if(rem <= 0) return 0;
    if(ind >= n) return 1e9;
    
    int ans = 0;
    for(int instance=0; instance<=rem; instance++){
        int curr
    }
}

int findd(vector<int> a, vecyor<int> b, int m){
    int n = a.size();
    vector<vector<int>> dp(n, vector<int>(m, -1));
    
    int answer = find_dp(a, b, dp, m, 0, n);
    return answer;
}

int main()
{
        vector<int> a = {2,1,1};
        vector<int> b = {1, 2,3};
        m = 4;
        cout<<findd(a, b, m);
        return 0;
}

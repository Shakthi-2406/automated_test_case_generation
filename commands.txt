pip freeze > requirements_new.txt
pip download -r requirements.txt -d wheel_files
pip wheel -r requirements.txt --wheel-dir=wheel_files
pip install --no-index --find-links=wheel_files -r requirements.txt
